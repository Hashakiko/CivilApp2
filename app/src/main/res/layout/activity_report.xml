<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="#000000"
    android:padding="20dp">

    <!-- Title -->
    <TextView
        android:id="@+id/helloText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/hello_please_submit_any_report"
        android:textColor="#FFFFFF"
        android:textSize="20sp"
        android:layout_marginBottom="20dp" />

    <!-- Camera Icon (ImageButton to select image from camera/gallery) -->
    <ImageButton
        android:id="@+id/selectImageButton"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:src="@android:drawable/ic_menu_camera"
        android:contentDescription="@string/select_image"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:layout_marginBottom="20dp" />

    <!-- ImageView to show the selected image -->
    <ImageView
        android:id="@+id/selectedImageView"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_marginBottom="20dp"
        android:visibility="gone"/>

    <!-- EditText to enter a report or string -->
    <EditText
        android:id="@+id/reportEditText"
        android:layout_width="match_parent"
        android:layout_height="78dp"
        android:layout_marginBottom="20dp"
        android:background="#333333"
        android:hint="@string/enter_your_report"
        android:padding="10dp"
        android:textColor="#FFFFFF" />

    <!-- Submit Button -->
    <Button
        android:id="@+id/submitReportButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/submit_report"
        android:textColor="#FFFFFF" />
</LinearLayout>
